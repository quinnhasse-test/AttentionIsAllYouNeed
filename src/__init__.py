"""Attention Is All You Need — transformer from scratch.

Package structure:
  src.model       — transformer architecture (attention, encoder, decoder)
  src.data        — vocabulary, tokenizer, Multi30k dataset loader
  src.training    — loss, LR scheduler, training loop
  src.evaluation  — greedy/beam decoding, BLEU, attention visualization
"""
